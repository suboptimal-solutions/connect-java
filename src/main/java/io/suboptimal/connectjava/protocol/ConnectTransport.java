package io.suboptimal.connectjava.protocol;

/**
 * HTTP transport version a {@link ConnectChannelConfigurer} and its
 * {@link RoutingHandler} are wired for.
 */
enum ConnectTransport {
    HTTP_1_1,
    HTTP_2
}
